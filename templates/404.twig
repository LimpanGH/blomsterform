{% extends '_layouts/base.twig' %}

{% block content %}
  <section class="error-404">
    <div class="container">
      <h1>Oops! Page Not Found</h1>
      <p>Here are a few helpful links to get you back on track:</p>
      <ul>
        <li>
          <a href="{{ siteUrl('pottery') }}">Keramik</a>
        </li>
        <li>
          <a href="{{ siteUrl('about') }}">About</a>
        </li>
        <li>
          <a href="{{ siteUrl('contact') }}">Kontakt</a>
        </li>
        <li>
          <a href="{{ siteUrl() }}">Hem</a>
        </li>
      </ul>
      <p>
        If you're still having trouble, feel free to <a href="{{ siteUrl('contact') }}">reach out</a> to us directly!
      </p>
    </div>

    <form action="{{ siteUrl('search') }}" method="get">
      <input type="text" name="q" placeholder="Search our Pottery Collection..." />
      <button type="submit">Search</button>
    </form>
  </section>
{% endblock %}
